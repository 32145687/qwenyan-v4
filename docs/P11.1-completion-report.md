# Qianyan V4 · P11.1 Completion Report

> **阶段**: P11.1 Scaffold（Writing Workflow 的写作 Pipeline 最小骨架）
> **日期**: 2026-08-21
> **性质**: 实施 + 测试 + 构建验证（`:app:android` 因本环境无 Android SDK 而如实跳过）
> **结论**: **P11.1 = DONE**（Scaffold 边界建立，真实创作 NOT-implemented，不伪装能力）

---

## 1. 实现内容

P11.1 建立"写作 Pipeline 的最小领域/应用骨架边界"，使 P11.2~P11.5 可在稳定边界上继续实现。

- **core:model — 写作最小领域模型**（新增）
  - [`Draft`](file:///workspace/core/model/src/main/kotlin/com/qianyan/model/writing/WritingModels.kt)：写作正文草稿载体，含 `DraftId` / `novelId` / `variantId` / `scope`（按 `variantId` 推导 `ORIGINAL|VARIANT`）/ `chapterId?` / `planId?` / `content` / `status` / `sourceModel`（字符串承载，领域不依赖 provider）/ `createdAt` / `updatedAt`；`@Serializable`。
  - `DraftStatus`：`DRAFTING / WRITTEN / REVISED / FINAL`（最小）。
  - `DraftId`（[Ids.kt](file:///workspace/core/model/src/main/kotlin/com/qianyan/model/Ids.kt#L181-L186)）。
  - **不重复建模**：规划复用 `ChapterPlan`（story）、意图复用 `UserWritingRequest`（context）、评审复用 `ValidationResult`（spec）；不创建 `WritingContext / PlanningContext`（非必需）。

- **application — 写作 Use Case 骨架 + MiMo 后处理 seam**（新增）
  - [`WritingUseCases`](file:///workspace/application/src/main/kotlin/com/qianyan/application/usecase/writing/WritingUseCases.kt)：`plan / write / critique / revise` 四个入口存在但**一律抛类型化 `WritingScaffoldNotImplemented`**（诚实骨架，不返回假数据）；`postProcessDraft` 委托注入的 `WritingPostProcessor`（本阶段真实生效）。
  - [`WritingPostProcessor`](file:///workspace/application/src/main/kotlin/com/qianyan/application/usecase/writing/postprocessor/WritingPostProcessor.kt)：写作输出后处理 **seam**（接口位置），默认 `PassthroughWritingPostProcessor`（直通、不特判模型）。MiMo 专用算法 **DEFER P11.5**，不污染 Provider / AgentRuntime / Engine。

- **application — 类型化骨架错误 + DI**
  - `ApplicationError.WritingScaffoldNotImplemented`（新增，[ApplicationError.kt](file:///workspace/application/src/main/kotlin/com/qianyan/application/error/ApplicationError.kt#L95-L99)）。
  - [ApplicationContainer.kt](file:///workspace/application/src/main/kotlin/com/qianyan/application/di/ApplicationContainer.kt#L75-L76)：暴露 `writing: WritingUseCases`（可装配 + 可测试）。

## 2. 修改文件（tracked）

| 文件 | 变更 | 是否 P8/P9/P10 逻辑 |
|---|---|---|
| [ApplicationContainer.kt](file:///workspace/application/src/main/kotlin/com/qianyan/application/di/ApplicationContainer.kt) | +import `WritingUseCases`；+`val writing` getter | 否（纯新增暴露） |
| [ApplicationError.kt](file:///workspace/application/src/main/kotlin/com/qianyan/application/error/ApplicationError.kt) | +`WritingScaffoldNotImplemented` | 否（纯新增错误类型） |
| [Ids.kt](file:///workspace/core/model/src/main/kotlin/com/qianyan/model/Ids.kt) | +`DraftId` | 否（纯新增 ID） |
| [README.md](file:///workspace/README.md) | 同步 P11.1 状态，不超前标 P11.2+ | — |

## 3. 新增文件（untracked）

- [WritingModels.kt](file:///workspace/core/model/src/main/kotlin/com/qianyan/model/writing/WritingModels.kt)（领域）
- [WritingUseCases.kt](file:///workspace/application/src/main/kotlin/com/qianyan/application/usecase/writing/WritingUseCases.kt)（骨架）
- [WritingPostProcessor.kt](file:///workspace/application/src/main/kotlin/com/qianyan/application/usecase/writing/postprocessor/WritingPostProcessor.kt)（seam）
- [WritingModelsTest.kt](file:///workspace/core/model/src/test/java/com/qianyan/model/WritingModelsTest.kt)（3 tests）
- [WritingScaffoldTest.kt](file:///workspace/application/src/test/kotlin/com/qianyan/application/WritingScaffoldTest.kt)（6 tests）

> 另：`docs/P11-preflight-audit.md` 为上一只读审计产物（未跟踪，非 P11.1 实现）。

## 4. 架构变化

- 新增方向：`core:model`（writing 包）→ *（纯数据，无新依赖）*；`application` → `core:model`（复用 Draft/ChapterPlan/UserWritingRequest/ValidationResult）；`application` → `com.qianyan.application.usecase.writing`（编排 seam）。
- **无依赖方向违规**：core:model 零依赖；application 不依赖 provider:impl（只经装配注入 Mock 测试）；未触碰 agent/provider/storage/engine 模块。
- MiMo seam 归属 **Application 写作编排层**（非 provider:impl、非 AgentRuntime、非 core:engine）——符合架构硬约束。

## 5. 数据库变化

- **无 Schema 变化、无 migration、无新增 Repository 接口/实现**。
- 正文持久化（`Draft` → SQLDelight/Repository）**DECISION：DEFER P11.3**（在真实写作发生时落地，避免"无调用方的 Repository/迁移"这一过度设计与 migration 风险；revision 语义继续服从 P8.2/P8.3 冻结）。

## 6. 测试

| 模块 | 新增 | 结果 |
|---|---|---|
| core:model `WritingModelsTest` | 3（ORIGINAL/VARIANT 作用域推导、默认值、序列化往返） | ✅ |
| application `WritingScaffoldTest` | 6（装配、plan/write/critique/revise 抛类型化骨架错误、postProcess 直通） | ✅ |
| P8/P9/P10 既有测试 | 未改动 | ✅ 全量 JVM 回归通过 |

## 7. Build

- **JVM 全量测试**：`:core:model` /`:core:engine` /`:storage` /`:provider:api` /`:provider:impl` /`:agent:tool` /`:agent:runtime` /`:agent:agents` /`:agent:orchestration` /`:runtime` /`:application` → **BUILD SUCCESSFUL**。
- `git diff --check` → **PASS**。
- **Android（`:app:android:testDebugUnitTest` / `assembleDebug`）**：**如实跳过** —— 本环境无 Android SDK（`local.properties` 的 `sdk.dir` 指向不存在目录），失败于 SDK 解析。**未伪造结果**；JVM 验证与静态审计已完成。上一已验证基线（P9/P10 + Android BUILD SUCCESSFUL）见 README 注。

## 8. 未实现内容（按阶段纪律，属于后续 P11.x）

- `plan / write / critique / revise` 真实执行（**P11.2 / P11.3 / P11.4**）。
- Agent（Intent/Planner/Writer/Critic/Knowledge）与 `AgentRuntime` 与业务 Tool 的实际接线（**P11.2+**）。
- Draft 持久化 / Repository / migration（**P11.3**）。
- Revision 循环（`revisionCount≤3` 已有，未接）（**P11.4**）。
- MiMo 写作后处理算法（仅 seam 位置，**P11.5**）。
- Knowledge Update / Final Review / E2E Writing slice（**P11.5**）。
- Android/Desktop UI、HITL、Retry、异步、流式、真实 Provider 新件（**不属 P11.1**）。
- `TaskRunner` **未放开** WRITING/PLANNING（P11.1 不需要；P8.3 对 `WRITING/PLANNING/KNOWLEDGE_UPDATE` 抛 `UnsupportedTaskType` 行为与测试保持不变）。

## 9. Known Issues / Risks

- Gradle wrapper 分发下载在本环境超时；使用本地已装 Gradle 8.14.5 + 代理系统属性（`JAVA_TOOL_OPTIONS` 注入 `http/https.proxyHost=127.0.0.1:18080`）完成构建。对仓库无影响。
- Android SDK 缺失 → Android 构建未在本次重跑（非代码问题）。
- `WritingScaffoldNotImplemented` 尚未在 `ErrorMapper` 注册（其不经 Storage/引擎映射而来，直接由 UseCase 抛出，因此无需映射）。若后续在 `TaskRunner`/Workflow 捕获，再审视是否需要映射。

## 10. 下一阶段前置条件（P11.2）

- 建议以 `plan` 为 P11.2 起点：新增 planner `AgentContract` + 从 `UserWritingRequest` 组装写作上下文（复用 `ContextCandidate`/`ContextType`），经 `AgentRuntime`（Mock gateway）产出 `ChapterPlan` 并 `saveCheckpoint` 落盘。
- `WritingUseCases.postProcessDraft` seam 已就绪，P11.5 替换为 MiMo 特判实现。
- 正文持久化决策（新表 vs MemoryEntry 过渡）需在 P11.3 落地前在 P11.2 末锁定。

## 11. Git 状态

- **本次未 commit / 未 push**（按指令）。
- 变更：3 tracked（Container/Error/Ids）+ README + 5 untracked（模型/骨架/seam/测试）+ 1 untracked 审计文档。
- `git diff --check` = PASS；无 force push；无 P8/P9/P10 核心逻辑改动。